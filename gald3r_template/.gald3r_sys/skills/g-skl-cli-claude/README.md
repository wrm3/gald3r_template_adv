# g-skl-cli-claude
**Skill file**: `SKILL.md`

> Human-facing companion to `SKILL.md`. The LLM agent reads `SKILL.md`; this page is for developers browsing the skill library.

## What it does

Claude Code CLI (claude command) — headless flags, session continuation, MCP config, permissions, multi-agent via Agent SDK, and overnight/CI best practices.

## When to use

- Invoke via `@g-skl-cli-claude` (or when the agent determines this skill is relevant)
- See the **When to Use** / trigger section of `SKILL.md` for the authoritative list

## Related skills

- See `SKILL.md` and the gald3r skill index for related skills
